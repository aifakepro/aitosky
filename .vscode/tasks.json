{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Deploy GitHub",
      "type": "shell",
      "command": "bash",
      "args": [
        "-c",
        "git add . && git diff --cached --quiet || git commit -m \"update\" && git push"
      ]
    }
  ]
}